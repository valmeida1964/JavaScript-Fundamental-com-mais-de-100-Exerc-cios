/* 
    Objetos.
    1. crie um objeto chamado student com as seguintes propriedades:
        name: String
        classes: Array

    2. Atribua seus dados ao objeto
    NOTA: a propriedade classes deve ser um array contendo os seguintes 
    elementos: 'Math', 'English', 'Programming'

    3. Exiba o objeto inteiro no console
*/

let student = {
    name: "João",
    classes: ['Math', 'English', 'Programming']
};

console.table(student);
// console.table(student.classes);