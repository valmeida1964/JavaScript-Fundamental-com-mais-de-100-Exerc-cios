// Vamos criar uma função que recebe um array e um índice 
// e exibe o nome no console.
function showName(names, index) {
    console.log(names[index]);
}

let names = ["John", "Paul", "Ringo", "George"];

for (let index = 0; index < names.length; index++) {
    showName(names, index);
}

// Este código não é o melhor exemplo de aplicação de funções.
// É apenas um exemplo.
